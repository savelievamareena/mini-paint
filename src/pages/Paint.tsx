import { Box } from "@mui/material";
import { Canvas } from "../modules/Canvas.tsx";

const Paint = () => {
    return (
        <Box>
            <Canvas></Canvas>
        </Box>
    );
};

export default Paint;
